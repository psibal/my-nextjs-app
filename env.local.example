# Database
DATABASE_URL="postgresql://user:password@ep-name.region.aws.neon.tech/dbname?sslmode=require"

# NextAuth (required)
AUTH_SECRET="your-secret-key-generate-with-openssl-rand-base64-32"

# Google Auth (OPTIONAL - only set if using Google OAuth)
# Uncomment these lines if you want to enable Google authentication
# AUTH_GOOGLE_ID="your-google-client-id.apps.googleusercontent.com"
# AUTH_GOOGLE_SECRET="your-google-client-secret"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="My Next.js App"

# Authentication Settings
# Set to "true" to require authentication for dashboard access
# Set to "false" to allow public dashboard access (read-only for non-authenticated users)
REQUIRE_AUTH="false"

# Demo Mode - Allow anyone to create/edit/delete posts without authentication
# SECURITY WARNING: Only enable this for demos, local development, or if you don't need user accounts
# Set to "true" to allow anonymous post management (no sign-in required)
ALLOW_ANONYMOUS_POSTS="true"
